# Large re-fetchable inputs. Every one is listed in MANIFEST.tsv with its source URL
# and SHA-256, and scripts/run_all.sh reconstructs them, so they are catalogued but
# not redistributed.
data/counts/
data/runsheets/
data/chip/
data/tf_prior/*.tar
data/tf_prior/*.gtf.gz
data/tf_prior/*.txt.gz
data/tf_prior/tair_gene_aliases.txt
data/tf_prior/tf_gene_edges.tsv
data/tf_prior/weighted/
data/osdr/arabidopsis_factor_values.csv
vendor/*.jar

# Shenzhou-8 raw arrays (ArrayExpress E-MTAB-2518) and the RMA helper script 25 writes.
data/arrayexpress/*.CEL
data/arrayexpress/*.annot.gz
data/arrayexpress/_rma.R

# B. rapa orthology: proteomes, DIAMOND databases and raw hit tables are all rebuilt by
# scripts/26_brapa_orthologs.py. The RBH map itself IS versioned -- it is small, it is the
# actual product, and the BioMart route that would otherwise regenerate it is broken.
data/brapa/*.fa
data/brapa/*.fa.gz
data/brapa/*.dmnd
data/brapa/fwd.tsv
data/brapa/rev.tsv

# DREM run products: model.txt and the parsed tables are versioned, the bulky
# intermediates are not.
results/drem/inputs/
results/drem/smoke/
results/drem/runs/*/tfscores/

# Build and environment
__pycache__/
*.pyc
.java_path
manuscript/latex/*.aux
manuscript/latex/*.bbl
manuscript/latex/*.blg
manuscript/latex/*.log
manuscript/latex/*.out
manuscript/latex/*.fdb_latexmk
manuscript/latex/*.fls
manuscript/latex/*.toc
.DS_Store
